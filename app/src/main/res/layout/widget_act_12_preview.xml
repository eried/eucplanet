<?xml version="1.0" encoding="utf-8"?>
<!--
  Picker preview for the two-button widget. Same reasoning as the single: the
  real layout previews as two horns, which is neither what the widget will show
  nor a hint that the actions are the rider's to pick.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:baselineAligned="false">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/widget_bg"
        android:padding="8dp">
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginBottom="6dp"
            android:src="@drawable/ic_widget_configure"
            android:contentDescription="@null" />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#FFFFFFFF"
            android:maxLines="2"
            android:ellipsize="end"
            android:text="@string/widget_act_1_short" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_marginStart="6dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/widget_bg"
        android:padding="8dp">
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginBottom="6dp"
            android:src="@drawable/ic_widget_configure"
            android:contentDescription="@null" />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#FFFFFFFF"
            android:maxLines="2"
            android:ellipsize="end"
            android:text="@string/widget_act_2_short" />
    </LinearLayout>
</LinearLayout>
