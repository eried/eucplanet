⚙️ First-launch fixes: language and units now detect
   correctly on every fresh install.
🎨 Theme picker: custom themes no longer collide with
   built-ins; save warns before replacing an existing one.
✨ Minor polish.
