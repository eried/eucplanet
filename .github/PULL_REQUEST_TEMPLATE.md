Base branch: `next-experimental`. Not `main`. See CONTRIBUTING.md.

## What it changes

## Tested on

Wheel and firmware, or "emulator only". Attach a btsnoop capture for
protocol changes.

## Checklist

- [ ] Builds with `gradlew.bat :app:assembleDebug`
- [ ] Tests for the touched code pass
- [ ] New rider-facing text is in `strings.xml` for every locale
- [ ] No em-dashes
